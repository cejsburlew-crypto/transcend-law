{
  "version": 2,
  "buildCommand": "cd transcend-frontend && npm install --legacy-peer-deps && npm run build && cd .. && cp -r transcend-frontend/dist/* .",
  "outputDirectory": ".",
  "installCommand": "npm ci --legacy-peer-deps",
  "env": {
    "NODE_ENV": "production"
  },
  "functions": {
    "api/**/*.ts": {
      "memory": 512,
      "maxDuration": 30,
      "runtime": "nodejs20"
    }
  },
  "routes": [
    { "src": "/api/attorneys(.*)", "dest": "/api/attorneys.ts", "methods": ["GET"] },
    { "src": "/api/notaries(.*)", "dest": "/api/notaries.ts", "methods": ["GET"] },
    { "src": "/api/(.*)", "dest": "/api/$1.ts", "methods": ["GET", "POST", "PUT", "DELETE"] },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
