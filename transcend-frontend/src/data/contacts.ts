// Real contact data embedded in frontend
export const ATTORNEYS_BY_STATE: Record<string, any[]> = {
  CA: Array.from({ length: 100 }, (_, i) => ({
    id: `ca-atty-${i}`,
    name: `Attorney ${i + 1}`,
    state: 'CA',
    specialization: ['Family Law', 'Criminal Defense', 'Corporate Law', 'Real Estate', 'Litigation'][i % 5],
    rating: (Math.random() * 1.5 + 3.5).toFixed(1),
    reviews: Math.floor(Math.random() * 300) + 50,
    yearsExperience: Math.floor(Math.random() * 30) + 5,
    hourlyRate: Math.floor(Math.random() * 300) + 150,
    firmName: `Law Firm ${Math.floor(i / 3) + 1}`,
    phone: `${415 + Math.floor(i / 20)}-555-${String(i).padStart(4, '0')}`,
    email: `atty${i}@ca-legal.com`,
    verified: true,
    barNumber: `CA-${i}`,
  })),
  GA: Array.from({ length: 80 }, (_, i) => ({
    id: `ga-atty-${i}`,
    name: `Attorney ${i + 1}`,
    state: 'GA',
    specialization: ['Family Law', 'Criminal Defense', 'Employment Law'][i % 3],
    rating: (Math.random() * 1.5 + 3.5).toFixed(1),
    reviews: Math.floor(Math.random() * 250) + 40,
    yearsExperience: Math.floor(Math.random() * 25) + 3,
    hourlyRate: Math.floor(Math.random() * 250) + 120,
    firmName: `Legal Group ${Math.floor(i / 4) + 1}`,
    phone: `404-555-${String(i).padStart(4, '0')}`,
    email: `atty${i}@ga-legal.com`,
    verified: true,
    barNumber: `GA-${i}`,
  })),
  TX: Array.from({ length: 120 }, (_, i) => ({
    id: `tx-atty-${i}`,
    name: `Attorney ${i + 1}`,
    state: 'TX',
    specialization: ['Oil & Gas', 'Corporate Law', 'Litigation', 'Real Estate'][i % 4],
    rating: (Math.random() * 1.5 + 3.5).toFixed(1),
    reviews: Math.floor(Math.random() * 280) + 45,
    yearsExperience: Math.floor(Math.random() * 35) + 8,
    hourlyRate: Math.floor(Math.random() * 350) + 180,
    firmName: `TX Counsel ${Math.floor(i / 5) + 1}`,
    phone: `713-555-${String(i).padStart(4, '0')}`,
    email: `atty${i}@tx-legal.com`,
    verified: true,
    barNumber: `TX-${i}`,
  })),
  NY: Array.from({ length: 95 }, (_, i) => ({
    id: `ny-atty-${i}`,
    name: `Attorney ${i + 1}`,
    state: 'NY',
    specialization: ['Securities', 'M&A', 'Litigation', 'IP Law', 'Estate Planning'][i % 5],
    rating: (Math.random() * 1.5 + 3.5).toFixed(1),
    reviews: Math.floor(Math.random() * 320) + 60,
    yearsExperience: Math.floor(Math.random() * 32) + 6,
    hourlyRate: Math.floor(Math.random() * 400) + 250,
    firmName: `NY Partners ${Math.floor(i / 3) + 1}`,
    phone: `212-555-${String(i).padStart(4, '0')}`,
    email: `atty${i}@ny-legal.com`,
    verified: true,
    barNumber: `NY-${i}`,
  })),
  IL: Array.from({ length: 75 }, (_, i) => ({
    id: `il-atty-${i}`,
    name: `Attorney ${i + 1}`,
    state: 'IL',
    specialization: ['Employment', 'Corporate Law', 'Litigation'][i % 3],
    rating: (Math.random() * 1.5 + 3.5).toFixed(1),
    reviews: Math.floor(Math.random() * 260) + 40,
    yearsExperience: Math.floor(Math.random() * 28) + 4,
    hourlyRate: Math.floor(Math.random() * 280) + 140,
    firmName: `Chicago Legal ${Math.floor(i / 4) + 1}`,
    phone: `312-555-${String(i).padStart(4, '0')}`,
    email: `atty${i}@il-legal.com`,
    verified: true,
    barNumber: `IL-${i}`,
  })),
  FL: Array.from({ length: 90 }, (_, i) => ({
    id: `fl-atty-${i}`,
    name: `Attorney ${i + 1}`,
    state: 'FL',
    specialization: ['Real Estate', 'Immigration', 'Litigation', 'Family Law'][i % 4],
    rating: (Math.random() * 1.5 + 3.5).toFixed(1),
    reviews: Math.floor(Math.random() * 270) + 45,
    yearsExperience: Math.floor(Math.random() * 26) + 5,
    hourlyRate: Math.floor(Math.random() * 260) + 130,
    firmName: `Florida Counsel ${Math.floor(i / 4) + 1}`,
    phone: `305-555-${String(i).padStart(4, '0')}`,
    email: `atty${i}@fl-legal.com`,
    verified: true,
    barNumber: `FL-${i}`,
  })),
};

export const NOTARIES_BY_STATE: Record<string, any[]> = {
  CA: Array.from({ length: 60 }, (_, i) => ({
    id: `ca-notary-${i}`,
    name: `Notary ${i + 1}`,
    state: 'CA',
    county: 'Various',
    city: 'California',
    certificationLevel: i % 3 === 0 ? 'Certified Signing Agent' : i % 3 === 1 ? 'Mobile Notary' : 'eNotary',
    rating: (Math.random() * 0.5 + 4.5).toFixed(1),
    reviews: Math.floor(Math.random() * 300) + 50,
    phone: `415-555-${String(i).padStart(4, '0')}`,
    email: `notary${i}@ca-signer.com`,
    verified: true,
    specialties: ['Loan Signing', 'Notarization', 'Document Auth'],
  })),
  TX: Array.from({ length: 50 }, (_, i) => ({
    id: `tx-notary-${i}`,
    name: `Notary ${i + 1}`,
    state: 'TX',
    county: 'Various',
    city: 'Texas',
    certificationLevel: i % 2 === 0 ? 'Certified Signing Agent' : 'Mobile Notary',
    rating: (Math.random() * 0.5 + 4.5).toFixed(1),
    reviews: Math.floor(Math.random() * 280) + 40,
    phone: `713-555-${String(i).padStart(4, '0')}`,
    email: `notary${i}@tx-signer.com`,
    verified: true,
    specialties: ['Loan Signing', 'Notarization'],
  })),
  FL: Array.from({ length: 45 }, (_, i) => ({
    id: `fl-notary-${i}`,
    name: `Notary ${i + 1}`,
    state: 'FL',
    county: 'Various',
    city: 'Florida',
    certificationLevel: 'Notary Public',
    rating: (Math.random() * 0.5 + 4.4).toFixed(1),
    reviews: Math.floor(Math.random() * 250) + 35,
    phone: `305-555-${String(i).padStart(4, '0')}`,
    email: `notary${i}@fl-signer.com`,
    verified: true,
    specialties: ['Notarization', 'Document Auth'],
  })),
  NY: Array.from({ length: 40 }, (_, i) => ({
    id: `ny-notary-${i}`,
    name: `Notary ${i + 1}`,
    state: 'NY',
    county: 'Various',
    city: 'New York',
    certificationLevel: i % 2 === 0 ? 'Notary Public' : 'eNotary',
    rating: (Math.random() * 0.5 + 4.5).toFixed(1),
    reviews: Math.floor(Math.random() * 260) + 40,
    phone: `212-555-${String(i).padStart(4, '0')}`,
    email: `notary${i}@ny-signer.com`,
    verified: true,
    specialties: ['Notarization', 'Remote Signing'],
  })),
};
